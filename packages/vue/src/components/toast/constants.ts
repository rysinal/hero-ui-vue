/** Gap between stacked toasts, in pixels. */
export const DEFAULT_GAP = 12
/** How many toasts are visible before the rest collapse behind them. */
export const DEFAULT_MAX_VISIBLE_TOAST = 3
/** Scale applied to each toast further back in the stack. */
export const DEFAULT_SCALE_FACTOR = 0.05
/** Default toast width, in pixels. */
export const DEFAULT_TOAST_WIDTH = 460
/** Default auto-dismiss timeout, in milliseconds. */
export const DEFAULT_TOAST_TIMEOUT = 4000
